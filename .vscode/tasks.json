{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build",
            "command": "mkdir -p build && cmake -B build && make -C build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}